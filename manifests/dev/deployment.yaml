apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-deploy
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: my-app
        image: minhaz71/argocd-multicluster-demo:dev-1.0.0
        ports:
        - containerPort: 3000